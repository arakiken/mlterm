prefix=@prefix@
mandir=@mandir@

DESTDIR=
MANDIR=$(DESTDIR)$(mandir)
MAN1DIR=$(MANDIR)/man1

INSTALL=@INSTALL@

all .DEFAULT:
	@echo "no such a target"

$(MAN1DIR):
	mkdir -p $(MAN1DIR)

install: $(MAN1DIR)
	$(INSTALL) -m 644 mlterm.1 $(MAN1DIR)
